.log__info{data: {message: {id: "#{message.id}"}}}
  .log__info__user
    %span.log__info__user__name
      = message.user.name
    %span.log__info__user__time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .log__info__message
    - if message.text.present?
      = message.text
      %br
    = image_tag message.image_url if message.image_url present?